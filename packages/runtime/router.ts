import type { AdRunnerManifest, CandidateOutcome, CandidateResult, Placement } from "../specification/types.js"; import { getAdapter, type RuntimeContext } from "../adapters/index.js";
export interface RouteResult { placement:string; filled:boolean; attempts:Array<{unit:string; outcome:CandidateOutcome; reason?:string; elapsedMs:number}>; }
export class PlacementRouter { constructor(private manifest:AdRunnerManifest, private emit:(name:string,detail:Record<string,unknown>)=>void){} async route(placement:Placement,target:Element,context:RuntimeContext):Promise<RouteResult>{ const attempts:RouteResult["attempts"]=[]; target.textContent=""; this.emit("slot-requested",{placement:placement.id,anchor:placement.anchor}); for(const [i,c] of placement.candidates.entries()){ const unit=this.manifest.units[c.unit]; const network=unit&&this.manifest.networks[unit.network]; if(!unit||!network?.enabled) continue; const adapter=getAdapter(network.adapter); const container=document.createElement("div"); container.dataset.adRunnerCandidate=c.unit; target.appendChild(container); const started=performance.now(); this.emit("candidate-started",{placement:placement.id,anchor:placement.anchor,unit:c.unit,network:unit.network,candidateIndex:i}); const controller=new AbortController(); let timer:number|undefined; try{ if(adapter.load) await adapter.load(network,context); const result=await Promise.race<CandidateResult>([adapter.mount(unit,container,context,controller.signal),new Promise(r=>{timer=window.setTimeout(()=>r({outcome:"timeout",reason:"candidate timeout"} as CandidateResult),c.timeout_ms);})]); const elapsedMs=Math.round(performance.now()-started); attempts.push({unit:c.unit,outcome:result.outcome,reason:result.reason,elapsedMs}); this.emit(`candidate-${result.outcome}`,{placement:placement.id,anchor:placement.anchor,unit:c.unit,network:unit.network,candidateIndex:i,outcome:result.outcome,elapsedMs}); if(result.outcome==="filled"){ target.querySelectorAll(":scope > [data-ad-runner-candidate]").forEach(el=>{if(el!==container) el.remove();}); if(c.guaranteed) this.emit("fallback-used",{placement:placement.id,unit:c.unit,network:unit.network}); this.emit("slot-filled",{placement:placement.id,anchor:placement.anchor,unit:c.unit,network:unit.network,outcome:"filled",elapsedMs}); return {placement:placement.id,filled:true,attempts}; } controller.abort(); await adapter.destroy?.(unit,container,context); container.remove(); } catch(e){ const elapsedMs=Math.round(performance.now()-started); attempts.push({unit:c.unit,outcome:"error",reason:String((e as Error).message),elapsedMs}); this.emit("candidate-error",{placement:placement.id,unit:c.unit,network:unit.network,candidateIndex:i,outcome:"error",elapsedMs}); controller.abort(); await adapter.destroy?.(unit,container,context).catch(()=>{}); container.remove(); } finally{ if(timer) clearTimeout(timer); } } this.emit("slot-collapsed",{placement:placement.id,anchor:placement.anchor}); target.textContent=""; return {placement:placement.id,filled:false,attempts}; }}
